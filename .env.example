# Gemini API Key for Magic Enhance feature
# Get your API key from https://aistudio.google.com/app/apikey
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# Optional: Web OAuth client ID used by launchWebAuthFlow fallback.
# Recommended for development/unpacked extension auth fallback.
# If omitted, fallback is disabled and extension uses chrome.identity.getAuthToken only.
VITE_GOOGLE_OAUTH_CLIENT_ID=your_google_oauth_client_id.apps.googleusercontent.com
